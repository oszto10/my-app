import React from "react";

function Pizza({ name, id, ingredients, picture, prize }) {
  /*   const pizzaName = pizzaData.name;
  const  = pizzaData */

  return (
    <div>
      <h1>{name}</h1>
    </div>
  );
}

export default Pizza;
